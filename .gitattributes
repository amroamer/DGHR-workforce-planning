# Treat binary assets as binary — never apply line-ending conversion.
*.xlsx binary
*.xls binary
*.pdf binary
*.png binary
*.jpg binary
*.docx binary

# Shell scripts run in Linux containers/VMs — force LF regardless of contributor OS.
*.sh text eol=lf
backend/entrypoint.sh text eol=lf
